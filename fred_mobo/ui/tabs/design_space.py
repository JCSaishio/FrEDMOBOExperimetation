"""Design-space window (§7.3 item 4): heat-maps over the design plane of log alpha, posterior mean and sd of each outcome, and Pr(F), with evaluated points, failures and the next candidate overlaid.

Milestone M8. No computation — display only.
"""

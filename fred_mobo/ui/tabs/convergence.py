"""Convergence window (§7.3 item 8): HV_n, HVhat_n and exp(log alpha*)/HV_n against n, with the thresholds drawn and the 'converged — export' prompt.

Milestone M8. No computation — display only.
"""

"""PySide6 user interface — milestone M8.

§9.2 item 11: nothing here may be built until the backend runs end-to-end from the CLI on
the emulator with all tests passing. §7.3: the UI is a thin client. It calls only
``CampaignController`` and contains no computation.
"""

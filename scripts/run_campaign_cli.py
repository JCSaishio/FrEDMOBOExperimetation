"""Headless, seeded campaign driver — milestone M7.

Drives a full emulator campaign (intake, declare, fit, propose, converge, second target
session, export) with no UI. Acceptance: runs unattended in under 5 minutes on CPU and
produces the export files.
"""

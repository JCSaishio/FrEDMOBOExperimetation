"""Acceptance tests for milestone M1.

CampaignConfig JSON round-trip; validation rejects a config with mismatched variable and bound counts.

Placeholder: no tests yet. M0 is layout only — see docs/PLAN.md.
"""

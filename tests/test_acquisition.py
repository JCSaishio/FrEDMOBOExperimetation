"""Acceptance tests for milestone M5.

the objective transform produces |d - d*| from samples of d; qLogNEHVI builds against the installed BoTorch signature; no candidate has Pr(F) < 0.05 once the constraint GP has >= 8 points.

Placeholder: no tests yet. M0 is layout only — see docs/PLAN.md.
"""

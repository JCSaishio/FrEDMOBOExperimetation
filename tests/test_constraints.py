"""Acceptance tests for milestone M5.

constraint callables return the (11) convention (negative = feasible); Pr(F) grid matches the analytic band probability.

Placeholder: no tests yet. M0 is layout only — see docs/PLAN.md.
"""
